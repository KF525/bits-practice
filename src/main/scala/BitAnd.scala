class LargestAnd {

  def findLargestAnd(n: Int, k: Int) = {
    val intList = (1 to n).toList
    val returnAnd =
  }

  def returnAnd()

}


/*
Binary AND Operator copies a bit to the result if it exists in both operands.
largest where A&B from list < k
0001
0010
0011
0100
0101
*/